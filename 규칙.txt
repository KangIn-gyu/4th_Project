
1. 카멜 케이스를 사용한다
2. 전역 변수일 경우 g_ 표시를 한다
3. 클래스 맴버 함수와 변수를 작성할때는 아래과 같이 설정한다
{  public 함수 -> protected 함수 -> private 함수 -> public 변수 -> protected 변수 -> private 변수 }
4. 인터페이스일 경우 I를 표시한다
5. 함수 파라미터 _표시 하기
6. D3D 같은 경우 앞에 대문자로 된게 몇개 있음
7. 매크로 같은 경우 대문자로 작성
8. 업데이트 순서는 Start -> Update(Loop) -> LateUpdate(Loop)

// 각자 수정요청 및 전달사항 여기에 작성해도 됨

// 24. 12. 27
샘플러 생성 해당 샘플러는 Renderer가 가지고 있음 2개 만듬

// 24.12.29
윈도우 정보를 다이렉트클래스에 포인터로 들고 있게 처리함. 창변경 같은 처리할때 너무 불편해서 아싸리 들고 있게함
Get에 맴버 변환 못 시키게 const 작업 진행
카메라 오브젝트라고 오브젝트를 상속한 클래스 생성
메테리얼, 매쉬, 셰이더의 플로우 차트 정리 못함
임풋 레이아웃 고민중.. 생성에 대한 원소를 미리 구조체로 정의해서 사용하게 해야 되나... 
모델 클래스를 따로 만들자 컴포넌트와 별개 아직 안함

// 24.12.30
메테리얼 여기 안에 텍스쳐를 가지는 구조로 만들자 이게 맞는거 같다
리소스 매니저 일부 수정 자동으로 Resource폴더에 접근하도록 함 내부에는 각 스테이지별로 폴더를 나누었으며, 
스테이지 폴더에는 FBX, Materials, Shaders, Sounds, Texturs 이렇게 폴더를 가지고 있다.이렇게 나눈 것은 
추후 스테이지 폴더를 전체 올로드를 통해서 한번에 리소스 처리를 하기 위해서 이다.
리소스 매니저 tga, png, dds 파일 확인해본 결과 매우 잘됨

// 24.12.31 새벽
FBX 로더 설계중
스테틱 매쉬, 스켈레탈 매쉬 기반만 잡아 놓음

// 24.12.31 
// fbx 60퍼 진행
// 