1. 카멜 케이스를 사용한다
2. 전역 변수일 경우 g_ 표시를 한다
3. 클래스 맴버 함수와 변수를 작성할때는 아래과 같이 설정한다
{  public 함수 -> protected 함수 -> private 함수 -> public 변수 -> protected 변수 -> private 변수 }
4. 인터페이스일 경우 I를 표시한다
5. 함수 파라미터 _표시 하기
6. D3D 같은 경우 앞에 대문자로 된게 몇개 있음
7. 매크로 같은 경우 대문자로 작성
8. 업데이트 순서는 Start -> Update(Loop) -> LateUpdate(Loop)
입력 처리 -> 카메라 업데이트 -> 라이트 업데이트 -> 몬스터/NPC 업데이트
플레이어 업데이트 -> 충돌 감지 및 물리 처리 -> 애니메이션 업데이트 -> 렌더링
9. Resourece 폴더는 실행파일이 있는 곳에 넣어야 됨. 현재 실행 파일이 있는 폴더는 4th_Project에 있다
만약 빌드를 하면 빌드 폴더에 넣어야 됨.
10 . // TODO: 작업필요한거 주석으로 표시

깃 규칙
1. 커밋하기전에 수신 패치를 통해서 다른 사람이 작업을 했는지 확인한다. 만약 수신에 내용이 있다면 그걸 풀 받고선 커밋 후 푸시를 하자
2. 내용은 되도록이면 자세히 적어서 올리자
3. 마스터에 병합할때는 다같이 하자

<각자 수정요청 및 전달사항 여기에 작성해도 됨>

// 24. 12. 27
샘플러 생성 해당 샘플러는 Renderer가 가지고 있음 2개 만듬

// 24.12.29
윈도우 정보를 다이렉트클래스에 포인터로 들고 있게 처리함. 창변경 같은 처리할때 너무 불편해서 아싸리 들고 있게함
Get에 맴버 변환 못 시키게 const 작업 진행
카메라 오브젝트라고 오브젝트를 상속한 클래스 생성
메테리얼, 매쉬, 셰이더의 플로우 차트 정리 못함
임풋 레이아웃 고민중.. 생성에 대한 원소를 미리 구조체로 정의해서 사용하게 해야 되나... 
모델 클래스를 따로 만들자 컴포넌트와 별개 아직 안함

// 24.12.30
메테리얼 여기 안에 텍스쳐를 가지는 구조로 만들자 이게 맞는거 같다
리소스 매니저 일부 수정 자동으로 Resource폴더에 접근하도록 함 내부에는 각 스테이지별로 폴더를 나누었으며, 
스테이지 폴더에는 FBX, Materials, Shaders, Sounds, Texturs 이렇게 폴더를 가지고 있다.이렇게 나눈 것은 
추후 스테이지 폴더를 전체 올로드를 통해서 한번에 리소스 처리를 하기 위해서 이다.
리소스 매니저 tga, png, dds 파일 확인해본 결과 매우 잘됨

// 24.12.31 새벽
FBX 로더 설계중
스테틱 매쉬, 스켈레탈 매쉬 기반만 잡아 놓음

// 24.12.31 
// fbx 60퍼 진행

// 25. 1. 2
// 모델 로드 성공
// 오브젝트 타입 추가
// 카메라 오브젝트 생성시에는 자동으로 타입이 카메라로 됨

// 25. 1. 3
// 드디어 메테리얼 생성 후 텍스처 로드 다 성공
// fbx 예외 처리 해야 한다. 해당 맵에 키값이 있다면?? 종료하고 없다면 생성하게 처리해야 한다\

// 25. 1. 5
// 리소스 시스템은 작동으로 파일 패치 입력을 Resource을 붙여준다
// aiNode 트리 구조로 생성 후 깊은 복사로 맵에서 준다.
// 미친... 인덱스 버퍼랑 버텍스 버퍼 매쉬 이름이 같아서 안지우는 경우가 생김 문제 확인이 됨 수정이 필요
// 렌더러 컴포넌트에 고민이 많이 됨

// 25. 1. 6
// 버퍼 삭제 안되는 거 수정 완료
// 불필요한 wstring 제거 대부분 들고 있는건 string으로 처리 만약 wstirng으로 변환이 필요할 경우만 그렇게 함
// 렌더러 컴포넌트 일부 적립
// 렌더러 클래스 50퍼 작업 완료
// 어심프에 있는 타입과 레지스터 번호 맞게 처리함 텍스처가 해당 인덱스를 가지고 있음
// 카메라 작업중
// 오브젝트 매니저 테스트 중(아직 기반클래스에 불과)

// 25. 1. 7 해야 할 것
// 카메라 오브젝트 작업중인거 마무리
// 오브젝트 매니저 기능 구현 
// 렌더러 클래스 상수 버퍼 값 전달 마무리 하기(카메라 끝나면 가능)

// 9시 40분 중간 수정한 내용 
// 씬을 통해 오브젝트 생성하여 그림 작업 진행중 현재 FBX 로더에서 트랜스폼에 문제가 있다
// 오브젝트 매니저에서 메인 카메라 세팅하는 함수 추가 해당 카메라는 카메라 오브젝트에 있는 스태틱 맴버이다
// 임풋 시스템의 IinputProcesser 이걸 상속한 객체가 삭제되면 임풋시스템의 벡터에서 삭제되는 구조로 변경

// 25.1.8
// 드디어 모델 텍스처는 없지만 모델이 나오기 시작했다
// FBX AiNode를 이상하게 관리해서 문제가 많았음. 복사와 이동에 대해서 좀더 공부가 필요함
// 해야 할것 IMGUI 만들기

// 25.1.9
// ImGui 기초 기반 생성
// 카메라 생성 
// 현재 씬이랑 오브젝트 매니저 기능의 혼용되고 있다 이거 기준을 잡아야 할 거 같다
// 씬 작업중 


// 25. 1. 13
// 씬 생성과 동시에 씬매니저에 담겨진다.
// 씬을 통해서 오브젝트 뛰우기 성공
// 콘솔창 변경 imGui로 처리하자 

// 25.1. 14
// 시간 없어서 걍 imgul 스테틱 라이브러리에서 그냥 엔진으로 이동시킴 그리고 싱글톤으로 만듬
// 